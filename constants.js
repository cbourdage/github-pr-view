
// local storage keys
export const STORAGE_PREFIX = 'github-pr-view';
export const STORAGE_PRS_KEY = `${STORAGE_PREFIX}-prs`;
export const STORAGE_TOKEN = `${STORAGE_PREFIX}-token`;

// cache
export const CACHE_TTL = 15 * 60 * 1000;
